{
  "name": "neopass",
  "version": "0.5.1",
  "description": "A password validation and generation tool kit",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc && npm run load",
    "build-test": "tsc",
    "clean": "rm -rf lib",
    "coverage": "nyc mocha",
    "codecov": "npm run coverage && codecov -t $CODECOV_TOKEN",
    "load": "source .env && node ./util/file-loader ./src/views ./lib",
    "test": "npm run build && npm run coverage",
    "test-watch": "nodemon -e ts --ignore '*.d.ts' --exec 'mocha'",
    "prepublishOnly": "npm run clean && npm run test",
    "word-gen": "./word-gen"
  },
  "homepage": "https://github.com/neopass/neopass#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/neopass/neopass.git"
  },
  "bugs": {
    "url": "https://github.com/neopass/neopass/issues"
  },
  "keywords": [
    "password",
    "passphrase",
    "validation",
    "strength",
    "topology",
    "middleware"
  ],
  "author": "James Abney",
  "license": "MIT",
  "devDependencies": {
    "@types/mocha": "^5.2.3",
    "@types/node": "^10.5.8",
    "codecov": "^3.3.0",
    "mocha": "^5.2.0",
    "nodemon": "^1.17.5",
    "nyc": "^14.0.0",
    "source-map-support": "^0.5.12",
    "ts-node": "^8.1.0",
    "tslint": "^5.10.0",
    "typescript": "^3.4.2"
  },
  "files": [
    "lib"
  ],
  "dependencies": {
    "regex-each": "^1.0.3"
  }
}
